---
import { CAL_LINKS } from '../lib/calcom';

const stats = [
  { value: '160€', label: 'por sessão · 1-2 pessoas' },
  { value: '60min', label: 'duração da sessão' },
  { value: '5–10', label: 'fotos finais editadas' },
  { value: 'Privada', label: 'individual ou casal' },
];
---

<section class="relative border-t border-cream-50/8 bg-night-900 py-16 sm:py-20">
  <div class="mx-auto max-w-7xl px-5 sm:px-8">
    <p class="eyebrow reveal mb-4">Descrição e Preço</p>
    <p class="reveal max-w-2xl text-base leading-relaxed text-cream-50/75 sm:text-lg" style="--reveal-delay: 0.05s">
      Uma sessão privada de fotografia de retrato sob o céu estrelado, no observatório. Guiada do
      início ao fim, com exposição correta para o céu e para ti ao mesmo tempo — uma técnica pouco
      comum entre fotógrafos de retrato.
    </p>

    <dl class="reveal mt-10 grid grid-cols-2 gap-x-8 gap-y-8 sm:grid-cols-4" style="--reveal-delay: 0.1s">
      {
        stats.map((s) => (
          <div class="border-l border-accent-400/30 pl-4">
            <dt class="sr-only">{s.label}</dt>
            <dd class="text-3xl font-bold tracking-tight text-accent-200 sm:text-4xl">{s.value}</dd>
            <dd class="mt-1 text-xs font-medium uppercase tracking-[0.14em] text-cream-50/55">
              {s.label}
            </dd>
          </div>
        ))
      }
    </dl>

    <a
      href={CAL_LINKS.retratos}
      target="_blank"
      rel="noopener"
      class="btn btn-primary reveal mt-10"
      style="--reveal-delay: 0.15s"
    >
      Reservar sessão
    </a>
  </div>
</section>
